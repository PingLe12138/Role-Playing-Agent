# RPA 角色扮演 · 标志设计说明

> 标志名称：**剧场星轨 R（Theatre Orbit R）**
> 定位：RPG 多智能体叙事引擎的视觉字标，继承系统既有品牌色（`--brand-500: #5d9eff`、导航深蓝 `#0f1b33`）。

---

## 一、概念

「编排多角色演出一段故事」是这个系统的本质，标志取自三个意象的交汇：

| 意象 | 对应系统 | 图形语言 |
|------|----------|----------|
| **环形剧场/环幕** | 单线叙事的剧情循环（一轮轮导演→表演→审查推进） | 一个圆环轨 |
| **剧中人物** | `sessionPresentCharacter` 在场角色（actor 节点） | 环上 **三个白点** |
| **戏中玩家** | HITL 玩家选择决策点（`player_choice_node`） | 环上一颗**琥珀星** |
| **导演编排** | LangGraph 双层图（Supervisor → Director） | 中心的 **R** 由线条构成 |

中心 R 刻意用**线段 + 圆头笔触**绘制——它就是一张微型示意图：竖笔是导演中枢，上部的圈是角色回路，向右下的斜腿是「剧情流向玩家」。整个 R 与环上节点的「点-线」语言完全同构，呼应系统「图编排」的底色，而不只是字母缩写。

## 二、结构解析（主标 96×96）

```
          ┌──────────────┐
           ○             ← 顶：角色点（蓝 #3d78e0 衬入白环）
         ●    R   ★      ← 右上：琥珀星（玩家决策点）
          ○     ○        ← 左下 / 右下：角色点
          └──────────────┘
```

- **环轨**：圆心 (48,48)，半径 30，白色，96% 不透明度——叙事循环。
- **三点**：顶部 (48,19)、左下 (22.6,62.5)、右下 (73.4,62.5)，r=4.6，主标用**蓝点**、透明版用**白心蓝圈**衬入环轨，同时是省略号，暗示剧情未完待续。
- **琥珀星**：位于环轨右上角（中心 (73.1,33.5)，与右下角色点关于水平轴对称），五角星贴环而坐、白描边 1.6——玩家始终「在场」于故事轨道上，是环上唯一的分叉入口（HITL）。
- **中心 R**：`stroke 4.8` 圆头笔触，高 25、宽 18.5，居中。

## 三、色板

| 名称 | 色值 | 用途 |
|------|------|------|
| 品牌蓝 A | `#6aa9ff` | 徽章渐变的亮端（菜单高亮 `#5d9eff` 同族） |
| 品牌蓝 B | `#2f66d8` | 徽章渐变的深端（透明版的环/R） |
| 节点蓝 | `#3d78e0` | 主标角色节点（衬入白环轨） |
| 琥珀 | `#FFC066`（主标）/ `#F5A623`（透明版） | 玩家星——唯一暖色，暗示「聚光灯」 |
| 白 | `#FFFFFF` | 徽章版内元素、透明版节点心 |

渐变方向 135°（左上→右下），与顶栏光晕、页卡高亮一致；徽章圆角 22（R 占位符旧版为 8px 圆角方块，新标保留「圆角方块」家族记忆）。

## 四、版本与文件

| 文件 | 用途 |
|------|------|
| `frontend/src/assets/logo.svg` | **主标**：蓝渐变徽章版，深色/深蓝背景（顶栏、favicon、分享封面） |
| `frontend/src/assets/logo-mark.svg` | **透明版**：浅色背景（文档页、登录页、配置页、打印） |
| `frontend/public/favicon.svg` | 浏览器标签页图标（与主标同源） |

已接入的位置：

- **顶栏**：`AppLayout.vue` 用 `<img :src="logoUrl">`（30px，接替原「蓝底白 R」占位符）。
- **favicon / 页面标题**：`index.html`（`<title>RPA 角色扮演</title>` + `favicon.svg`）。

## 五、应用规则（简易）

1. 徽章版应**整块使用**，不裁剪、不反白、不旋转；小尺寸（≥24px）时星与 R 仍可辨，不建议再缩小。
2. 透明版在浅色背景上使用；若深色背景请改用主标。
3. 不允许给标志加额外描边/阴影（顶栏的柔和 drop-shadow 是唯一允许的外部效果）。
4. 任何衍生图形（横幅、海报）应先保证环轨与 R 的辨识，再谈装饰。

## 六、下一步（可选）

- 生成 512/1024 PNG 版用于 README 头图与分享封面（需要时再导出）。
- 登录页加入透明版与大写标语；`README.md` 顶部横幅可选嵌入。
